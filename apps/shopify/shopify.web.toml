# Web configuration for Remix (full-stack)
# Based on official shopify-app-template-remix

name = "remix"
roles = ["frontend", "backend"]

[commands]
predev = "npx prisma generate"
dev = "npx prisma migrate deploy && npm exec remix vite:dev"
